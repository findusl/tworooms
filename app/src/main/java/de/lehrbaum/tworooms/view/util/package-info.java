/**
 * This package contains all UI classes that are NOT activities and fragments that get instantiated.
 * <br>
 * Effectively it on one side contains any Adapters and similar classes that are only necessary to
 * provide the correct user experience to the user. On the other side it contains Activities and
 * Fragments that are superclasses for other classes in the view package.
 */
package de.lehrbaum.tworooms.view.util;